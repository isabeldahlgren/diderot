services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: diderot
      POSTGRES_USER: diderot
      POSTGRES_PASSWORD: diderot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
