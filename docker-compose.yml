services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: openauthor
      POSTGRES_USER: openauthor
      POSTGRES_PASSWORD: openauthor
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
